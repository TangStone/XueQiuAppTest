SearchAlibaba:
  step:
    - id: "search_input_text"
      send: ${keyword}
    - id: "name"

cancle:
  step:
    - id: "action_close"

addOpitionalStock:
  step:
    - id: "follow_btn"

getCurrentPrice:
  step:
    - id: "current_price"
      attribute: "TEXT"